
  # Bus List Design

  This is a code bundle for Bus List Design. The original project is available at https://www.figma.com/design/rTI1oVuDLxiIV4CpcJthPM/Bus-List-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  