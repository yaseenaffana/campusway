/*
  Deprecated compatibility file.

  SecondPassword is now part of the canonical schema/bootstrap in:
    backend/create_table.sql

  Keep this file only as a pointer for older workflows.
*/

PRINT 'Deprecated migration: SecondPassword is managed by backend/create_table.sql.';
